; Keep the desktop clean by default. The finish page still offers an explicit
; "Create a desktop shortcut" checkbox, while Start Menu creation is handled
; on its own page and remains enabled unless the user opts out.
!define MUI_FINISHPAGE_SHOWREADME_NOTCHECKED
